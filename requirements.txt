# Core
fastapi>=0.118.0
uvicorn[standard]>=0.37.0
pydantic>=2.11.0
pydantic-settings>=2.7.1

# Database (psycopg v3 ha wheel Windows per Python 3.14)
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
alembic>=1.14.0

# Data
pandas>=2.2.3
numpy>=2.2.0
fredapi>=0.5.2
requests>=2.32.3
feedparser>=6.0.11
# Asset prices (free): yfinance per Yahoo Finance, pyarrow per cache parquet
yfinance>=0.2.40
pyarrow>=15.0.0

# Machine learning (HMM regime classifier)
# NOTA: hmmlearn richiede compilatore C su Python 3.14 Windows (no wheel).
# Usiamo una implementazione Baum-Welch pure-numpy + scikit-learn GaussianMixture
# per l'inizializzazione. Scipy e' necessario (logsumexp).
scikit-learn>=1.5.0
scipy>=1.14.0
statsmodels>=0.14.0  # Markov-Switching regression (Hamilton 1989)

# Scheduler
apscheduler>=3.10.4

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx>=0.28.0

# Utils
python-dotenv>=1.0.1
loguru>=0.7.3
